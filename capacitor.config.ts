import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kblanks.healthhelper',
  appName: 'Health Helper',
  webDir: 'build'
};

export default config;
